<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Extra quantity email template -->
        <record id="extra_qty_notification_email" model="mail.template">
            <field name="name">Extra Quantity Received: Send by email</field>
            <field name="model_id" ref="union_colmbo_washing_plant.model_stock_picking"/>
            <field name="subject">Extra Quantity Received</field>
<!--            <field name="email_from">{{ (object.user_id.email_formatted or user.email_formatted) }}</field>-->
<!--            <field name="partner_to">{{ object.partner_id.id }}</field>-->
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        Hello,
                        <br/>
                        <br/>
                        Received extra quantity
                        <br/>
                    </p>
                </div>
            </field>
<!--            <field name="report_template" ref="action_report_saleorder"/>-->
<!--            <field name="report_name">{{ (object.name or '').replace('/','_') }}</field>-->
<!--            <field name="lang">{{ object.partner_id.lang }}</field>-->
<!--            <field name="auto_delete" eval="True"/>-->
        </record>

        <!-- Quality Check Email Template -->
        <record id="quality_check_report_email_template" model="mail.template">
            <field name="name">Quality Check Report</field>
            <field name="model_id" ref="union_colmbo_washing_plant.model_ucwp_quality_check"/>
<!--            <field name="partner_to">{{}}</field>-->
            <field name="subject">{{ object.name }} Quality check report</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        Dear
                        <br/>
                        <br/>
                        Here is in attachment a quality check
                        <strong t-out="object.name or ''">QC/</strong>
                    </p>
                </div>
            </field>
            <field name="report_template" ref="union_colmbo_washing_plant.quality_check_report_action"/>
            <field name="report_name">{{ (object.name or '') }}</field>
<!--            <field name="lang">{{ object.partner_id.lang }}</field>-->
        </record>

    </data>
</odoo>